import Image from "next/image";
import ProfilePic from "@/app/components/ProfilePic";
import Button from "@/app/components/Button";

export default function SterlingEngine() {
  return (
    <main className=" font-LeagueSpartan bg-black0-base w-screen h-full min-h-screen">
      <section className="w-[60%] h-full mx-auto pt-32">
        <h1 className="text-8xl font-semibold text-center pb-16">ASRS Project</h1>
        <p>Some Description</p>
      </section>
    </main>
  );
}
